'use strict';
// Public configuration only. Never put a Supabase secret/service-role key here.
// Empty values keep the game fully playable with device-only collections.
window.FIRST_DESCENT_CLOUD=Object.freeze({url:'',publishableKey:''});
